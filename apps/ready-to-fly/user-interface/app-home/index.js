'use strict';
const { authorizationScreen } = require('./authorization-screen');
const {
    thanksScreen
} = require('./travel-requests');

module.exports = {
    authorizationScreen,
    thanksScreen
};
